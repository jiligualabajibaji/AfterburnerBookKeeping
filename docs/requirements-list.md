# 记账 Agent — 需求清单

1. 我想做一个记账的Agent，包含寻常的记账功能，也可以用自然语言向AI描述并自动记账，用python语言，是Andriod应用
2. 这些方案都有什么区别？
3. 选方案B
4. 我自己用，可以不加登录功能、云功能，但是得支持账单导出，可以在另一个手机上的这款应用导入并识别
5. 日常流水记账 — 记每天花了多少（餐饮、交通、购物），月底看花了多少
6. 先实现文字输入，语音输入后面再添加
7. AI 解析后弹窗确认
8. 本地规则+可选 AI
9. 用方案B
10. 我不仅需要用自然语言描述记账的功能，还需要寻常记账软件的功能：用户选择选项，添加描述，填入花钱/赚钱数额，保存/修改
11. 需要用户可以自定义添加和删除类型选项
12. 只记录人民币，不需要其他币种；除了花销还有收入的关键词
13. 记账时可以只选择日期，不选择时分秒；对于语音记账，不说就自动不记录时分秒，记录到日期即可
14. 日期选择器可以选年月日，也可以记录到时分秒
15. 添加一条：如果AI api也没解析出来可以填的数据，就要像用户询问不清楚的字段
16. 对于"设置"的内容，添加一些主题、字号的选择
17. 添加"点击具体项目进行编辑"的功能
18. 在分类管理中，我删除了某些分类，显示"删除后该分类下的账单不会自动删除"，但是实际上是删除了的，我希望能将删除了的类别统一归类到"其他"这一类别中，且"其他"这一类别不可删除
19. 添加支出和收入的时候，希望分类能在屏幕的上半边显示，直接选类别，不需要再点击二级选项选择类型
20. 在不操作1分钟左右，会自动退出回到桌面
21. 在统计界面，AI解析得到的输入没有统计进去；在统计界面加一个对收入的统计
22. 希望能自定义图标，软件名称修改为：Jiliguala的记账软件
23. 图标在主目录下
24. AI解析的时候，希望能解析用汉字表达的数字
25. 在设置中添加用户能自己上传图片作为软件图标的功能，而且能够保存以往以往的最多10个图片，方便切换
26. 在手机桌面启动这个应用之后，会黑屏1秒，能不能加快启动过程？
27. 在统计界面，支出的饼状图用暖色系的颜色，收入的饼状图用绿色系的颜色
28. 上传的图片应该允许用户剪裁，而且上传之后不会变成软件的图标，在桌面上看还是默认的图标
29. 在用户点击某个记录准备编辑的时候，让该记录周围发光，如果是收入就发绿色的光，如果是支出就发红色的光
30. 在统计界面添加针对每一年的统计
31. 在记一笔的界面，右上角的保存放到右下角，并改为再记一笔的功能，也就是记完这一笔不返回主界面而是回到这一界面的默认状态（和刚刚那一笔的日期一致）；在再记一笔按钮的左边加一个按钮，是原来的保存功能
32. 在记一笔的界面，当记录的是支出，整个界面用红色的，按钮也用红色系的；当记录的是收入，整个界面用绿色的，按钮用绿色系的
33. 在记一笔的界面，选择分类的那一块，默认选择第一个分类；在设置界面，要求可以调整分类的顺序
34. 希望主界面中右下角的添加记一笔的绿色加号能设置为收入，再加一个红色的加号，设置为支出
35. 右下角的加号和减号的按钮长宽设置为原来的1.5倍大小
36. 上传图片后，软件会崩溃；添加对已上传图片的删除功能
37. 在设置界面的分类管理中，将支出和收入隔离开来
38. 在主界面添加"对一个记录向左滑动，右边显示删除的按钮"的功能
39. 统计界面中，年的支出和收入不正确，明明有记录，但是显示"暂无支出""暂无收入"
40. 统计界面，添加"点击月份或年份就可以选择具体的月份和年份，按照日历的格式来"
41. 在主界面长按一笔记录，不是整体发光，而是四周发光，而且发的光颜色不对
42. 在设置的分类管理中，支出的"其他"有两个，其中一个还可以删除，要求只保留一个，且改名为"其他支出"
43. 在统计界面的选择月份和年份时，弹出的日历显示到日期，修改为选择月份时弹出的界面只选择年份和月份，选择年份时弹出的界面只选择年份
44. 在记一笔的界面将"再记一笔"移到右上角，"保存并返回"移到右下角
45. 上传的图片应该允许用户剪裁（重提）
46. 在主界面向左滑动，弹出询问用户是否删除的界面有点慢了，能否在用户手指离开的瞬间就弹出这个界面？
47. 在主界面添加批量删除的功能
48. 在主界面不同的月份记一笔，默认日期不应该是今天，而是哪一个月份的1号
49. 现在自定义图标可以删除图片了，但是无法对上传的图片进行裁剪。要求添加裁剪功能（但裁剪功能导致闪退）
50. 选择图片后，直接闪退
51. 设置的分类管理中，添加分类无法选择添加收入的分类，要求在收入分类下面加入"添加收入分类"，在支出分类下面加入"添加支出分类"
52. 在主界面只要用户点击记录，就四周动态发光，而不是长按一秒后发光
53. 要求统计能够统计到小数点后两位（分），记录时最多记录到小数点后两位
54. 要求记一笔时，如果正处在今天所在的月份，就默认日期为今天，在别的月份，默认日期就设为当月1号
55. 要求取消长按发光的功能；要求对记录往左滑不是立刻删除，而是立刻弹出"是否删除"的界面
56. 现在在主界面点击某条记录，会四周发光，上面出现删除的选项，长按则会进入编辑界面，要求改一下，点击进入编辑界面，长按则什么操作也没有
57. 要求在主界面的日期的后边加一个"添加"的按钮，点击这个按钮就可以添加那一天的记录
58. 在统计界面的月份，点击月份，出现年份的选择，再点击年份，会出现日历的选择，这时应该出现月份的选择，且选择月份之后，就没了，不应该要选择日期；一并取消选择月份窗口最上面的英文日期选择
59. 添加在主界面左滑可以滑到上一个月的记录、右滑可以滑到下一个月的记录的功能
60. 删除记录的左滑是删除这个功能，在主界面最上面添加删除的按钮，点击这个按钮，可以选择要删除哪些记录，最后给用户确认已选择的记录
61. 用户上传的图标还是不能变成手机桌面显示的软件图标
62. 在统计界面，选择年份时，要求能够选择从2000到2100年；选择月份时，12个按钮的风格要和选择年份的一样，不要蓝色的
63. 在主界面，左右滑动还是不能流畅，而且在没有记录的月份还是无法滑动
64. 设置中的字号小中大没有任何作用
65. 在统计节目的月份选择每个月份的背景是白色的，要求和选择年份时的按钮一致
66. 取消主界面对于记录向左滑动为删除的功能
67. 在设置中，添加支出分类和添加收入分类的按钮点进去还可以切换类别，取消切换类别，不应该允许切换类别
68. 在统计中，在"月"的界面，如果点击月份，会先弹出选择年份再选择月份，要求能同时选择，左边一列是选年份，右边一列是选月份，可以上下滑动去选
69. 使用教程添加一点：选择日期时点击右边的日历符号可以添加具体时间，具体到分钟
70. 取消自定义图标的功能
71. 字号的切换还是没有实现
72. 主界面的日期变成黑色了，要求在黑色系统下是白色，白色系统下是黑色
73. 字号出bug了，软件现在无法打开，报错：'package:flutter/src/painting/text_style.dart': Failed assertion: line 996 pos 12:'fontsize != null || ........'
74. 生成可以安装的apk文件
75. 记一笔的时候，如果没填写金额，应该弹出一个提示告诉用户金额没有填写；把这个记录到requirements-list.md
76. 在主界面切换"流水"、"统计"、"设置"，希望在手指点下去的时候就切换，而不是松开手指再切换
77. 把设置中的分类管理添加到记一笔的界面，在选择分类中的最后加一个"+"的符号，点"+"可以添加一个类别
78. 从流水页面切换到别的也页面，再切回来会回到今天所在月份，应该回到用户之前浏览的月份
79. 如果没有填写金额，弹出的"请填写金额"不要点一次保存就弹出一次，设置2秒内只会弹出一次；另外，弹出的提示放到手机的上方，做的好看一点，原来太灰了不好看
80. 记一笔页面中，如果金额填写的不是数字，也弹出一个提示告诉用户应该填写数字
81. 在记一笔的页面，如果金额部分不对，弹出的弹窗在下面，要求修改到手机的上半部分
82. 在记一笔页面，填写金额支持加减算式，比如"100+1"，保存的时候自动计算，如果结果小于0，就弹出弹窗提示用户不可以小于0
83. 使用微信输入法在记一笔支出的时候，备注无法输入中文
84. 将弹出的窗口左右的大小缩小一点，不要靠到左右边界
85. 将弹窗缩小到比要说的话宽一点
86. 在统计中，比例的数字保留到小数点后两位数字，比如99.98%、99.00%；如果环内放不下，将数字用一根线引导外面标注
87. 在设置页面去除分类管理这个功能
88. 主界面的"+"和"-"(记一笔收入和支出)的透明度设置为33%
89. 在设置中添加功能：允许用户上传图片作为背景图
90. 增加软件启动速度，减小软件体积
91. 在AI记账方面，在AI解析旁边加一个功能"大模型解析"，原来的AI解析改为"规则解析"或者合适的名字，在两个按钮下方添加介绍：规则解析下面说"适合简单的句子"，"大模型解析"下面说"需要在设置中填写大模型API，支持复杂句记账"
92. 如果没有内容，弹出提示告诉用户；如果用户没有配置API，但是点击了"大模型解析"，就弹出提示告诉用户需要填写API，并询用户是否跳转到填写API的界面；在填写API的界面，接口地址举一个例子，比如deepseek
93. api接口地址，举的例子给真实可用的deepseek的地址
94. 设置的"AI API配置"改为"API配置"
95. 跳转的功能只设置了跳转到设置界面，再增加一个：展开"API配置"
96. 在AI记账中，如果要输入文字，会在输入键盘上面有一个警戒条，显示"BOTTOM OVERFLOWED BY 64 PIXELS"
97. 在AI记账中，如果输入无法被解析的句子，手机界面会变红，然后报错"NULL check operator used on a null value"
98. 在编辑某个记录时，右上角是删除标志，将这个界面改为和记一笔一样，有"再记一笔"
99. 将应用名改为"极速记账"
100. 在设置界面的api配置的接口地址的默认值设置为"https://api.deepseek.com"，例子设置为"例如deepseek：https://api.deepseek.com"；在下面再添加一项"模型"，默认值和例子设置为"deepseek-v4-flash或deepseek-v4-pro"
101. 如果我填了api，可以用"大模型解析"来解析复杂内容吗
102. 如果API配置不正确，有办法知道是APIkey、接口地址还是模型名填写错了吗
103. 保存了配置之后三个配置栏应该边为空，而且应该允许用户更改已经保存的配置
104. API配置中的示例全部居左
105. 在AI记账中，如果用户点击"去设置"，会跳转到设置界面，能否将API配置这一选项展开并高亮一秒钟
106. 从"去设置"转到API配置，高亮只存在于"API配置"这几个字，应该这一整块的背景都高亮
107. 对于接口地址和模型名，如果使用deepseek-v4-flash，应该如何填写
108. 模型的默认值和举例改为deepseek-v4-flash或deepseek-v4-pro；在切换到设置界面、展开API配置时，四个填写字段都应该是空的，而现在情况是显示了已保存的配置
109. 使用"大模型解析"会真使用大模型去解析吗
110. 对于规则解析，如果最后省略了"钱"、"元"等字眼，也应该可以识别出来
111. 对于规则解析，如果最后加了标点符号，要自动忽略
112. 对于规则解析，修改功能无法使用
113. 要在设置的API配置中添加正在使用的配置的标志，告诉用户正在使用哪个配置
114. 在切换配置的时候，会显示竖着的保底红字报错：RIGHT OVERFLOWED BY 27 PIXELS；且切换配置时，在上面的四个空不应该显示具体内容，只有当编辑的时候才应该显示具体内容
115. 在设置的API配置中，某个配置使用中，别的配置会有一个绿色的勾，去掉这个勾，最右边的竖着的三个点时编辑和删除，改为删除符号，至于编辑功能，在点击配置的时候就进入编辑状态；这次试了一下大模型解析，我输入"晚饭一百块。"会弹出提示"API错误：HTTP 302"
116. 对于规则解析，如果最后省略了"钱"、"元"等字眼，也应该可以识别出来，比如："晚饭吃一百。"应该识别
117. 对于规则解析，如果最后加了标点符号，要自动忽略
118. 对于规则解析，修改功能无法使用
119. 在设置的API配置中，正在使用的只能有一个，而且要允许用户切换。对于大模型解析的功能，无法有效使用。
120. 在AI记账界面，增加一个跳转到设置界面配置API的按钮，一按这个按钮，就跳转并打开API配置并高亮
121. 我输入"晚饭吃了一百块。"使用大模型解析，显示API错误：接口地址可能被重定向，请检查模型名是否正确
122. 使用大模型解析可以解析出来名，但是点击保存却没有真正保存；将API配置的接口地址举例和默认值改为https://api.deepseek.com，模型改为deepseek-v4-flash和deepseek-v4-pro
123. 在AI记账方面，对大模型解析增加取消功能，允许用户取消当前的大模型解析
124. 还是无法保存；规则解析的时候，如果说了"昨天"，不会记录到昨天，而是会记录到今天，如果说了日期，只会记录到备注中，不会记录到正确的日期，修复。
125. 如果规则解析无法识别类型，就计入其他支出或其他收入中
126. 规则解析无法解析收入的钱
127. 主界面添加点击左上角月份选择年和月的功能
128. 规则解析中，我说"大前天"会解析到前天；大模型解析中，我说"大前天"，会识别不正确
129. 大模型解析的内容，如果不修改直接保存，不会保存，如果修改了，才会保存
130. 在AI记账的大模型解析下面备注：容易解析错误
131. 在主界面的删除功能改下添加全选本月，如果点击，那么将当前所在月的所有记录都选上
132. 在设置中添加功能：允许用户自定义软件名，30个字符以内
133. 在主页面的删除功能中添加全选某天支出和全选某天收入的功能，允许用户选择一些日期，效果是全选这些日期的所有支出记录或收入记录
134. 主界面左上角选年月和统计界面选年月的时候，有延迟，加一个动画，用户手指点击上去，显示波纹，松开，立刻打开选年月的界面
135. 点击年月的波纹效果不明显，换成明显的动画
136. 去除点击年月时的动画；更改软件名称之后，在详细信息里显示的还是原名称
137. 检查并测试导入和导出功能
138. 在教程中增加对背景图、更改软件名称的说明
139. 使用微信输入法在支出时写备注，还是无法写中文，先找原因不修改
140. 发现问题了，微信输入法从上面金额切换到下面备注时，无法输入中文，但是如果从没有键盘的状态切换到备注，是可以输入中文的，这一点在收入也不对，不仅仅是支出处
141. 要允许用户用键盘的回车键切换输入窗口
142. 微信输入还是不行（方案A）
143. 金额字段改成text键盘方案（实现中文输入但不弹出数字键盘）
144. 金额字段切换回数字键盘（放弃微信输入法兼容）
145. 在记一笔的页面的下面告诉用户金额输入支持算式
146. 统计页面的"点击年月切换"的提示删除；设置中背景图的提示"上传图片作为软件首页背景"修改为"上传图片作为背景图片"
147. 为记一笔的页面的金额输入增加汉字的规则解析，比如输入一百三十六可以解析为136，输入一块二毛七可以解析为1.27；添加提示：支持输入中文
148. 输入"一百八"，解析得到108，应该得到180
149. 为什么这一次apk有56MB，上一次只有25MB
150. 在记一笔界面，输入金额聚焦于数字键盘（不考虑微信输入的bug）
151. 金额输入下面的支持中文的提示删了，还回来
152. 点击金额的Next后跳到备注框，键盘自动切换到文本键盘（先unfocus再延迟50ms focus）
153. 再记一笔时同时unfocus两个焦点再聚焦金额，保证微信输入法切回数字键盘
154. 再记一笔与Next的键盘切换延迟从100ms缩短至50ms
155. 主界面列表排序改为按时间倒序、id倒序，保证最新记录在最上面
156. 再记一笔后自动聚焦金额输入框

## v1.2.0
157. 版本升级至v1.2.0